language: go
install: go get -v github.com/spf13/hugo
script:
  - hugo
  - cd public
  - git commit -am "autobuild"
  - git push
branches:
  only:
    - travis
    - master
notifications:
    email:
        on_failure: always
